# Dependencias do backend do SGCAS.
#
# Versoes fixadas de proposito: o que roda em producao tem de ser o mesmo que
# foi testado aqui. Para atualizar, mude a versao, rode os testes e commite.
#
#   python3 -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt

cryptography==50.0.0
Django==5.1.15
django-environ==0.14.0
djangorestframework==3.17.2
gunicorn==23.0.0
mozilla-django-oidc==5.0.2
Pillow==12.3.0
psycopg==3.3.4
psycopg-binary==3.3.4
PyJWT==2.13.0
requests==2.34.2
